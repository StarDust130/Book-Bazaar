/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "covers.openlibrary.org",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
